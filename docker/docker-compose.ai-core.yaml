name: ai-core

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    environment:
      - TZ=${TZ}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: ["gpu"]
    ports:
      - "11434:11434" # Ollama HTTP API (restrict in firewall if preferred)
    volumes:
      - /srv/ai/ollama:/root/.ollama:rw
    labels:
      - com.centurylinklabs.watchtower.enable=true

  honcho-api:
    image: ghcr.io/plastic-labs/honcho:latest
    container_name: honcho-api
    restart: unless-stopped
    entrypoint: ["sh", "/app/docker/entrypoint.sh"]
    ports:
      - "8000:8000"
    environment:
      - DB_CONNECTION_URI=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@10.10.1.20:5433/${HONCHO_DB:-honcho}
      - CACHE_URL=redis://:${REDIS_PASSWORD}@10.10.1.20:6379/0
      - CACHE_ENABLED=true
      - AUTH_USE_AUTH=true
      - AUTH_JWT_SECRET=${HONCHO_JWT_SECRET}
    healthcheck:
      test:
        [
          "CMD",
          "/app/.venv/bin/python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=2).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    labels:
      - com.centurylinklabs.watchtower.enable=true

  honcho-deriver:
    image: ghcr.io/plastic-labs/honcho:latest
    container_name: honcho-deriver
    restart: unless-stopped
    command: ["/app/.venv/bin/python", "-m", "src.deriver"]
    depends_on:
      honcho-api:
        condition: service_healthy
    environment:
      - DB_CONNECTION_URI=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@10.10.1.20:5433/${HONCHO_DB:-honcho}
      - CACHE_URL=redis://:${REDIS_PASSWORD}@10.10.1.20:6379/0
      - CACHE_ENABLED=true
      - AUTH_USE_AUTH=true
      - AUTH_JWT_SECRET=${HONCHO_JWT_SECRET}
    labels:
      - com.centurylinklabs.watchtower.enable=true
