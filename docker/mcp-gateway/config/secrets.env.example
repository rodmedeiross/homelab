# Copy to `secrets.env` (same folder) on the host — it is gitignored.
# Key must match the catalog `secrets:` name. Value = Honcho admin JWT
# (from ~/.honcho/config.json -> "apiKey").
honcho.api_key=eyJ...replace-with-your-honcho-admin-jwt...
