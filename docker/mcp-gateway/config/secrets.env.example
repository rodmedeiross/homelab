# Copy to `secrets.env` (same folder) on the host — it is gitignored.
# HONCHO_API_KEY is your Honcho admin JWT, found in ~/.honcho/config.json -> "apiKey".
HONCHO_API_KEY=eyJ...replace-with-your-honcho-admin-jwt...
