# Docker configuration for Homepage
# This tells Homepage how to connect to Docker for service discovery

willy:
  host: dockerproxy
  port: 2375

mobydick:
  host: { { HOMEPAGE_VAR_MOBYDICK_HOST } }
  port: 2375
